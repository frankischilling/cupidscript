#ifndef BANNER_H
#define BANNER_H

// Returns a starting offset that makes the banner appear "centered" initially.
int banner_initial_offset_for_center(const char *text, const char *build_info);

#endif // BANNER_H
