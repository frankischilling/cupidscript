// const bindings example

const pi = 3.14;
print("pi=" + pi);

const [x, y] = [10, 20];
print("x=" + x + ", y=" + y);

const {name, role} = {"name": "Ada", "role": "engineer"};
print("name=" + name + ", role=" + role);
